// It's a JS library for creating user interfaces(navbar etc)
// It't created by facebook

// react.dev (oficial website)

// Component(button,image,video,card) is a reusable piece of individual code(contains html,css,JS together) that can be rendered(displayed) anywhere in the screen
// React er vitore sob kisu component akare lekha hoy

// JSX a react er code lekha hobe

// -----JSX(JavaScript Extension Syntax)(It's not actual js)------

// It lets us write HTML directly inside JS
// JSX code k Babel transpile(converts) to JS.(JSX->Babel->JS)
// https://babeljs.io/(Babel website a jaoa jabe)

// ----Setup React in local machine-----

// Create React App(old tool to create react environment) VS Vite(new tool)
// VITE - It's comparatively faster

// https://vitejs.dev (to install react)

// After cd to the project file-> npm run dev (to run the vite)

// -----Flow of the React App-----
// App.jsx and App.css (single app component)
// index.html main page that renders in the browser(it contains a root and connected with main.jsx)
// main.jsx a onek kisu import kora hoy and ata App.jsx theke App component k niye 'root' a dey jeta index.html er dara page a render hoy
// All changes will be made in App.jsx file or new files to be created

// Highest level component holo App component
// Creating a component is very similar to write a function
// To rendering a component,we write them like a HTML tag where we want to render that
// <Title></Title> OR <Title/> (these 2 are the declaring syntax)

// ----Import-Export(2 vabe kora jay)-----
// default export and named export(default tai beshi use hoy)

// -----Writing Markups in jsx(read these things in documentation-react.dev a)-----
// 1.Return a single root element;
// 2.Close all the tags properly (like <img/>)--closing tag obossoi thakte hobe
// 3.Use camelCase in most of the cases(Attribute a class use kora jabena className use korte hobe)

// ------Curly Braces in jsx(read from documentation)------
// -> when we use {} in jsx, we can write pure js inside it

// -----Structuring Components------
// atlassian website



// -------Part 2 React------
//-------React Props-------
// It's like argument passing as functions that makes a component dynamic and more versatile
// additional information that are passed in the component -> they are known as props

// Main page er css index.css file a change kora jabe


// After downloading developer tools from react.dev site for chrome,
// -> we can inspect the home page and go to >> dropdown and select components to check all the components of the current react page